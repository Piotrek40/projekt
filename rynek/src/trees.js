// Lipy proceduralne przy fontannie (pień, konary, korona z brył i kart liści, falowanie). Flaga: ?notrees=1.
// Układ lokalny drzewa: początek u podstawy pnia, +y w górę. Geometria do W.B; kolizja addCircle. Wypełnia tor „plac".
export function buildTrees(W) {
  if (W.ctx.flags.notrees) return;
}
